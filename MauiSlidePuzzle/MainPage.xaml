<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiSlidePuzzle.MainPage"
             xmlns:custom="clr-namespace:MauiSlidePuzzle.CustomViews"
             xmlns:skia="clr-namespace:SkiaSharp.Views.Maui.Controls;assembly=SkiaSharp.Views.Maui.Controls"
             BackgroundColor="#C1B3FF">

    <Shell.TitleView>
        <Grid x:Name="PuzzleTitleView" Margin="20, 0, 20, 0"
              BindingContext="{Binding CurrentStageInfo}">

            <Label Text="{Binding StageName}"
                   FontSize="24" TextColor="White"
                   HorizontalOptions="Start" VerticalOptions="Center"/>


            <HorizontalStackLayout
                HorizontalOptions="End" VerticalOptions="Center">

                <HorizontalStackLayout.Resources>
                    <x:Double x:Key="IconSize">40</x:Double>
                </HorizontalStackLayout.Resources>

                <ImageButton Source="left.png"
                             HeightRequest="{StaticResource IconSize}" WidthRequest="{StaticResource IconSize}"/>

                <!--<Image Source="{Binding EmbeddedImagePath}"
                       HeightRequest="{StaticResource IconSize}" WidthRequest="{StaticResource IconSize}"/>-->

                <ImageButton Source="reload.png"
                             HeightRequest="{StaticResource IconSize}" WidthRequest="{StaticResource IconSize}"/>

                <ImageButton Source="right.png"
                                HeightRequest="{StaticResource IconSize}" WidthRequest="{StaticResource IconSize}"/>
            </HorizontalStackLayout>
            
        </Grid>
    </Shell.TitleView>

    <ContentPage.Resources>
        <Style TargetType="ImageButton">
            <Setter Property="BackgroundColor" Value="Transparent"/>
        </Style>
    </ContentPage.Resources>

    <VerticalStackLayout VerticalOptions="Center" BindingContext="{Binding CurrentStageInfo}">

        <Grid HeightRequest="80">
            <Image x:Name="CompletedMessage"
                    Source="completed.png"
                    HorizontalOptions="Center"
                    IsVisible="false"/>
        </Grid>

        <Grid HeightRequest="300" WidthRequest="300">

            <Border StrokeThickness="15"
                    Stroke="Black">
                <custom:SlidePuzzleView x:Name="MyPuzzleView" Source="puzzle.png"
                                        IsEnabled="False"/>
            </Border>

        </Grid>

        <Grid HeightRequest="80">
            <ImageButton x:Name="StartButton" 
                        Source="start.png" 
                        HorizontalOptions="Center"
                        IsVisible="false"/>
        </Grid>

        <Grid HeightRequest="80">
            <ImageButton x:Name="ResetButton" 
                        Source="reset.png" 
                        HorizontalOptions="Center"
                        IsVisible="false"/>
        </Grid>

    </VerticalStackLayout>
    
</ContentPage>
